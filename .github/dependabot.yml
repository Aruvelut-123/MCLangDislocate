version: 2
updates:
  - package-ecosystem: "pip"
    directory: "/"  # Path where requirements.txt or requirements/*.txt is located
    schedule:
      interval: "weekly"  # Can be daily, weekly, or monthly
    labels:
      - "dependencies"
    commit-message:
      prefix: "deps"
      include: "scope"
